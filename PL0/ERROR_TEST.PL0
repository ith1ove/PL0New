PROGRAM ERROR_TEST;
VAR A, B;
BEGIN
    A := 5;
    B := 3;
    
    // 测试单独的 ! 符号（应该报错）
    !;
    
    // 测试 # 符号（应该报错，因为现在是非法字符）
    #;
    
    // 测试原来的 <> 不等号（应该报错，因为现在不支持）
    IF A <> B THEN
        WRITE(1);
        
END.
