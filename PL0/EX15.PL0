program EX15;
const 
    half = 0.5;
real 
    x, y, sum, product;

begin
    readreal(x);
    readreal(y);
    
    sum := x + y;
    product := x * y;
    
    writereal(sum);
    writereal(product);
    writereal(half)
end.
